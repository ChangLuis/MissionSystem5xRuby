<div class="row margin-fixed">
	<div class="col-md-8 offset-md-2">
		<% flash.each do |_, msg| %>
				<div class="alert alert-info center" role="alert" id="fade-out-target">
					<%= msg %>
				</div>
		<% end %>
	</div>
</div>